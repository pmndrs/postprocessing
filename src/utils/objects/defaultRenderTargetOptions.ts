import { RenderTargetOptions } from "three";

/**
 * Reusable default render target options.
 *
 * @internal
 */

export const defaultRenderTargetOptions: RenderTargetOptions = /* @__PURE__ */ {
	depthBuffer: false
};
