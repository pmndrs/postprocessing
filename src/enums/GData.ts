/**
 * An enumeration of geometry data that is available to Effects.
 *
 * @group Enums
 */

export enum GData {
	COLOR = "color",
	DEPTH = "depth",
	NORMAL = "normal",
	ROUGHNESS = "roughness",
	METALNESS = "metalness",
	LUMINANCE = "luminance"
}
