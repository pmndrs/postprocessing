/**
 * An enumeration of geometry data that is available to Effects.
 *
 * @category Enums
 * @internal
 */

export enum GData {
	COLOR = "color",
	DEPTH = "depth",
	NORMAL = "normal",
	OCCLUSION = "occlusion",
	ROUGHNESS = "roughness",
	METALNESS = "metalness",
	EMISSION = "emission",
	LUMINANCE = "luminance"
}
