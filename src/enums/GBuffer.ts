/**
 * An enumeration of G-Buffer components.
 *
 * @group Enums
 */

export enum GBuffer {
	COLOR = "gbuffer.color",
	DEPTH = "gbuffer.depth",
	POSITION = "gbuffer.position",
	NORMAL = "gbuffer.normal",
	NORMAL_DEPTH = "gbuffer.normaldepth",
	VELOCITY = "gbuffer.velocity",
	LUMINANCE = "gbuffer.luminance",
	ROUGHNESS = "gbuffer.roughness",
	METALNESS = "gbuffer.metalness"
}
