{
	"extends": "../tsconfig.json",
	"compilerOptions": {
		"paths": {
			// Need to use bundle for tests because of custom shader imports.
			"postprocessing": ["../dist/index.js"]
		},
		"noEmit": true
	}
}
